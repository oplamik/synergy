A, B = map(int, input().split())

# Определяем первое четное число в диапазоне
start = A if A % 2 == 0 else A + 1

# Выводим все четные числа от start до B включительно с шагом 2
if start <= B:  # Проверяем, что диапазон не пустой
    print(*range(start, B + 1, 2))
else:
    print()  # Если четных чисел нет 
