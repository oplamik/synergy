n = int(input())  # Чтение количества чисел
arr = list(map(int, input().split()))  # Чтение массива чисел

if n > 0:
    # Перестановка элементов: последний элемент становится первым, остальные сдвигаются вправо
    new_arr = [arr[-1]] + arr[:-1]
    print(' '.join(map(str, new_arr)))
else:
    print('')  # На случай пустого массива (хотя по условию N >= 1)
